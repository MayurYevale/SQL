/****** Script for SelectTopNRows command from SSMS  ******/

use school
--SELECT TOP (1000) 
--      [TeacherID]
--      ,[StudentID],
--      s.firstname+' '+s.lastname [Student Name],
--	  t.firstname+' '+t.lastname [Teacher Name]
--  FROM Enrollments e
--  inner join teacher t on t.id = e.TeacherID
--  inner join Students s on s.id = e.StudentID
--  group by [TeacherID],[StudentID],s.firstName,s.lastName,t.FirstName,t.Lastname
     
     
  --select [TeacherID],[CourseID] from Enrollments order by [TeacherID]

  --  select distinct [TeacherID],[CourseID] from Enrollments order by [TeacherID]







